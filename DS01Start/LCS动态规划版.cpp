#include <iostream>
#include <string>
using namespace ::std;
string LCS(string A, string B)
{
    int n = A.size(), m = B.size();
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < m; j++)
            if (A[i] == B[j])
    }
}